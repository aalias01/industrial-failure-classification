# Render deployment (pip-based)
# Local dev: conda env create -f environment.yml
#
# The API uses XGBoost's native `pred_contribs=True` for per-prediction SHAP,
# so the `shap` library is intentionally NOT a runtime dependency — it stays in
# environment.yml for notebook plotting (waterfall, summary, beeswarm) only.
# This keeps the Render image small and avoids shap-vs-xgboost version coupling.

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.9
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
scikit-learn>=1.3.0
xgboost>=2.0.0
imbalanced-learn>=0.11.0
joblib>=1.3.0
